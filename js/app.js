
const KEY='amr_official_v1_data';
const defaultData={
 settings:{mainTitle:'AMR',subTitle:'人力特攻隊',slogan:'團結・合作・突破・共創榮耀',notice:'歡迎來到 AMR 人力特攻隊',baseStart:'2026-07-06',weekDays:7,showBefore:2,showAfter:5},
 teams:[
  {id:'t1',name:'五狼攏來隊',members:['天景','小怡君','立維','永朋']},
  {id:'t2',name:'瀚瀚瀚瀚得第一',members:['子瀚','靜萱','陳怡君','雅韻']},
  {id:'t3',name:'盈在起跑點',members:['可盈','恩慈','士誼','奎璿','美如']},
  {id:'t4',name:'陽光委任隊',members:['怡蒨','楚涵','永濂','瑀芯']},
  {id:'t5',name:'你來就隊',members:['巧云','乙榛','圜翰','建宏']}
 ],
 rules:[
  {id:'dream_night',name:'夢想起飛 / 夜創出席',points:2,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'friend_dream_night',name:'帶新朋友參加夢想起飛 / 夜創',points:3,team:true,personal:false,allowFriends:true,friendPoints:3},
  {id:'soft_attend',name:'通訊處軟性活動出席',points:2,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'friend_soft',name:'帶新朋友參加軟性活動',points:3,team:true,personal:false,allowFriends:true,friendPoints:3},
  {id:'exam_pass',name:'公司考通過',points:3,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'contract',name:'登錄 + 簽約',points:10,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'excellent',name:'參加優培',points:10,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'joint_member',name:'團隊共同運作增員',points:1,team:true,personal:false,allowFriends:false,friendPoints:0},
  {id:'first_touch',name:'網增接觸 / 新增名單 / 約訪',points:1,team:false,personal:true,allowFriends:false,friendPoints:0},
  {id:'soft_cop',name:'緣故約訪 COP / 軟性活動',points:1,team:false,personal:true,allowFriends:false,friendPoints:0},
  {id:'first_interview',name:'初次面談',points:5,team:false,personal:true,allowFriends:false,friendPoints:0},
  {id:'deep_interview',name:'深度面談',points:8,team:false,personal:true,allowFriends:false,friendPoints:0},
  {id:'self_cop',name:'自己參加 COP',points:8,team:false,personal:true,allowFriends:false,friendPoints:0},
  {id:'friend_cop',name:'帶新朋友參加 COP',points:8,team:false,personal:true,allowFriends:true,friendPoints:8}
 ],
 events:[], records:[],
 portalItems:[
  {id:'portal_supercup',order:1,name:'人力超級盃',icon:'🏆',desc:'競賽辦法・積分規則・獎勵制度',featured:true,image:'./assets/portal/supercup-1.jpeg',summary:'115年度人力發展超級盃，整合組織發展積分、成功增員積分、通訊處排行與獎勵重點。',gallery:['./assets/portal/supercup-1.jpeg','./assets/portal/supercup-2.jpeg','./assets/portal/supercup-3.jpeg','./assets/portal/supercup-4.jpeg','./assets/portal/supercup-5.jpeg'],links:[]},
  {id:'portal_dream',order:2,name:'夢想起飛',icon:'🚀',desc:'轉職故事・創業說明會・活動報名',featured:true,image:'./assets/portal/dream-cover.jpeg',summary:'放置夢想起飛、夜間創業說明會、講師介紹、報名資訊與活動照片。',gallery:['./assets/portal/dream-cover.jpeg'],links:[]},
  {id:'portal_newbie',order:3,name:'臨時帳號建立',icon:'👤',desc:'新人帳號・公會登錄・優培申請流程',featured:true,image:'./assets/portal/newbie-cover.jpeg',summary:'整理新人臨時帳號、公會、登錄、優培申請流程，讓主管與新人快速查詢。',gallery:['./assets/portal/newbie-cover.jpeg'],links:[]},
  {id:'portal_excellent',order:4,name:'優培制度',icon:'🌱',desc:'115優質人才培訓・四階段培育計畫',featured:true,image:'./assets/portal/excellent-cover.jpeg',summary:'優培制度、公文、四階段條件、獎金制度與維持資格重點。',gallery:['./assets/portal/excellent-cover.jpeg'],links:[{title:'優培制度公文 PDF',url:'./assets/portal/excellent.pdf'}]},
  {id:'portal_appoint',order:5,name:'委任制度',icon:'👔',desc:'主任・襄理・區經理委任條件',featured:true,image:'./assets/portal/appoint-1.jpeg',summary:'整理業務主任、襄理、區經理委任條件與必修課程。',gallery:['./assets/portal/appoint-1.jpeg','./assets/portal/appoint-2.jpeg','./assets/portal/appoint-3.jpeg'],links:[]},
  {id:'portal_exam',order:6,name:'考照大補帖',icon:'📚',desc:'公輔班・考試日期・准考證與成績查詢',featured:true,image:'./assets/portal/exam-1.jpeg',summary:'整理新人公會、外幣、投資型測驗、公輔班、准考證與成績查詢資料。',gallery:['./assets/portal/exam-1.jpeg','./assets/portal/exam-2.jpeg','./assets/portal/exam-3.jpeg','./assets/portal/exam-4.jpeg','./assets/portal/exam-5.jpeg'],links:[]}
 ]
};
let data=load(), page=new URLSearchParams(location.search).get('page')||'home', scoreMode='team', editId=null, friendCount=1, statOffset=null;
let scoreState={periodId:'',team:'',name:'',items:{},manual:0,note:'',showPeriod:false}, historyFilter={q:'',periodId:'',team:''};

function load(){try{let x=JSON.parse(localStorage.getItem(KEY));if(!x)return structuredClone(defaultData);x.settings={...defaultData.settings,...(x.settings||{})};let defaults=structuredClone(defaultData.portalItems);x.portalItems=(x.portalItems&&x.portalItems.length?x.portalItems:defaults).map((it,i)=>{let d=defaults.find(v=>v.id===it.id)||{};return {...d,...it,order:Number(it.order||d.order||i+1)}});defaults.forEach(d=>{if(!x.portalItems.some(it=>it.id===d.id))x.portalItems.push(d)});return x}catch(e){return structuredClone(defaultData)}}
function save(){localStorage.setItem(KEY,JSON.stringify(data))}
function uid(p='id'){return p+'_'+Math.random().toString(36).slice(2,9)}
function today(){let d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function now(){return new Date().toLocaleString()}
function parseDate(s){s=String(s||'').replace(/\//g,'-');let [y,m,d]=s.split('-').map(Number);return new Date(y,m-1,d)}
function fmtDate(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function addDays(d,n){let x=new Date(d);x.setDate(x.getDate()+n);return x}
function daysBetween(a,b){return Math.floor((parseDate(b)-parseDate(a))/86400000)}
function weekLen(){return Math.max(1,Number(data.settings.weekDays||7))}
function periodByOffset(offset){let s=addDays(parseDate(data.settings.baseStart),offset*weekLen()),e=addDays(s,weekLen()-1);return {id:'w'+offset,offset,start:fmtDate(s),end:fmtDate(e)}}
function currentOffset(){return Math.floor(daysBetween(data.settings.baseStart,today())/weekLen())}
function periodOfDate(d=today()){return periodByOffset(Math.floor(daysBetween(data.settings.baseStart,d)/weekLen()))}
function periodById(id){return periodByOffset(Number(String(id||'w0').replace('w',''))||0)}
function visiblePeriods(){let cur=currentOffset(),before=Number(data.settings.showBefore||2),after=Number(data.settings.showAfter||5),arr=[];for(let i=cur-before;i<=cur+after;i++)arr.push(periodByOffset(i));return arr}
function periodLabel(p){if(!p)return '全部';let clean=s=>{let [m,d]=s.slice(5).split('-').map(Number);return `${m}/${d}`};return `${clean(p.start)}–${clean(p.end)}`}
function daysLeftInPeriod(){let p=periodOfDate();return Math.max(0,daysBetween(today(),p.end)+1)}
function periodOptions(sel='',all=false){let opts=visiblePeriods().map(p=>`<option value="${p.id}" ${sel===p.id?'selected':''}>${periodLabel(p)}${p.offset===currentOffset()?'（本週）':''}</option>`).join('');return `${all?'<option value="">全部日期區間</option>':''}${opts}`}
function recordsByPeriod(pid){return !pid?data.records:data.records.filter(r=>r.periodId===pid)}
function toast(m){let t=document.createElement('div');t.className='toast';t.textContent=m;document.body.appendChild(t);setTimeout(()=>t.remove(),1800)}
function link(p){return `?page=${p}`}
function go(p){history.pushState(null,'',link(p));page=p;render()} window.go=go;
window.addEventListener('popstate',()=>{page=new URLSearchParams(location.search).get('page')||'home';render()});
function teams(){return data.teams}
function people(){return data.teams.flatMap(t=>t.members.map(m=>({name:m,team:t.name,teamId:t.id})))}
function teamOf(name){let p=people().find(x=>x.name===name);return p?p.team:''}
function teamObj(name){return data.teams.find(t=>t.name===name)}
function members(team){return (teamObj(team)?.members)||[]}
function rule(id){return data.rules.find(r=>r.id===id)||data.rules[0]}
function rulesFor(mode){return data.rules.filter(r=>mode==='team'?r.team:r.personal)}
function activeEvent(){return data.events.find(e=>e.active)||data.events[0]}
function eventRecords(eventId){return data.records.filter(r=>r.eventId===eventId)}
function rankTeams(records=data.records){let m={};teams().forEach(t=>m[t.name]=0);records.forEach(r=>{if(r.team in m)m[r.team]+=Number(r.points||0)});return Object.entries(m).map(([team,score])=>({team,score})).sort((a,b)=>b.score-a.score)}
function rankPeople(records=data.records){let m={};people().forEach(p=>m[p.name]={score:0,team:p.team});records.forEach(r=>{if(!m[r.name])m[r.name]={score:0,team:r.team};m[r.name].score+=Number(r.points||0)});return Object.entries(m).map(([name,x])=>({name,team:x.team,score:x.score})).sort((a,b)=>b.score-a.score)}
function scoreTotal(){let total=Number(scoreState.manual||0);rulesFor(scoreMode).forEach(r=>total+=Number(scoreState.items[r.id]||0)*Number(r.points||0));return total}
function shell(body){document.getElementById('app').innerHTML=hero()+body+fab();nav();bindForms()}
function hero(){return `<section class="hero"><div class="logo"><div class="amr">${data.settings.mainTitle}</div><h1>${data.settings.subTitle}</h1><p>${data.settings.slogan}</p></div></section>`}
function navLinks(){return [['home','首頁'],['qr','QR'],['rank','排行榜'],['portal','Portal'],['admin','管理']].map(x=>`<a href="${link(x[0])}" class="${page===x[0]?'active':''}">${x[1]}</a>`).join('')}
function nav(){document.getElementById('bottomNav').innerHTML=[['home','🏠','首頁'],['qr','📱','QR'],['rank','🏆','排行榜'],['admin','⚙️','管理'],['portal','⭐','Portal']].map(x=>`<a href="${link(x[0])}" class="${page===x[0]?'active':''}"><span>${x[1]}</span>${x[2]}</a>`).join('')}
function fab(){return page==='checkin'?'':`<button class="fab" onclick="openScoreSheet()">＋</button><div id="sheetMask" class="sheetMask" onclick="closeScoreSheet()"></div><div id="scoreSheet" class="scoreSheet"><div class="sheetHandle"></div><h2>新增積分</h2><button onclick="startScore('personal')">👤 個人加分</button><button onclick="startScore('team')">👥 團隊加分</button><button class="cancel" onclick="closeScoreSheet()">取消</button></div>`}
document.addEventListener('click',e=>{let a=e.target.closest('a[href^="?page="]');if(!a)return;e.preventDefault();go(new URLSearchParams(a.getAttribute('href').slice(1)).get('page')||'home')},true)
window.openScoreSheet=()=>{document.getElementById('sheetMask')?.classList.add('show');document.getElementById('scoreSheet')?.classList.add('show')}
window.closeScoreSheet=()=>{document.getElementById('sheetMask')?.classList.remove('show');document.getElementById('scoreSheet')?.classList.remove('show')}
window.startScore=m=>{scoreMode=m;scoreState.items={};editId=null;go('score')}
function rows(arr,type){return arr.map((r,i)=>`<div class="row"><div class="medal ${i==0?'one':i==1?'two':i==2?'three':''}">${i+1}</div><div><div class="name">${type==='team'?r.team:r.name}</div><div class="muted">${type==='team'?'團隊':r.team}</div></div><div class="score">${r.score}</div></div>`).join('')||'<div class="empty">尚無資料</div>'}
function portalItemsSorted(){return (data.portalItems||defaultData.portalItems).slice().sort((a,b)=>Number(a.order||999)-Number(b.order||999))}
function portalCards(limit=6){let items=portalItemsSorted().filter(x=>x.featured).slice(0,limit);return `<div class="portalGrid">${items.map(x=>`<a class="portalCard withImage" href="?page=portal&id=${x.id}">${x.image?`<img src="${x.image}" alt="${x.name}">`:`<span>${x.icon||'⭐'}</span>`}<div class="portalShade"><b>${x.icon||'⭐'} ${x.name}</b><em>${x.desc||''}</em></div></a>`).join('')}</div>`}
function home(){let cp=periodOfDate(),rec=recordsByPeriod(cp.id),tr=rankTeams(rec),pr=rankPeople(rec),newFriends=rec.reduce((n,r)=>n+(r.friends||[]).length,0),events=data.events.filter(e=>e.periodId===cp.id).length;shell(`<section class="card noticeCard"><h2>📢 最新公告</h2><div class="name">${data.settings.notice||'歡迎來到 AMR 人力特攻隊'}</div></section><section class="stats"><div class="stat">🟢 本週<b>${periodLabel(cp)}</b><div class="muted">距離下一週 ${daysLeftInPeriod()} 天</div></div><div class="stat">🏆 冠軍隊伍<b>${tr[0]?.team||'尚無'}</b></div><div class="stat">👤 本週 MVP<b>${pr[0]?.name||'尚無'}</b></div><div class="stat">👥 新朋友<b>${newFriends}</b><div class="muted">本週活動 ${events} 場</div></div></section><div class="grid2"><section class="card"><h2>團隊前三名</h2>${rows(tr.slice(0,3),'team')}</section><section class="card"><h2>個人前三名</h2>${rows(pr.slice(0,3),'person')}</section></div><section class="card"><h2>最新紀錄</h2>${data.records.slice(-5).reverse().map(recordCard).join('')||'<div class="empty">尚無紀錄</div>'}</section><section class="card"><h2>⭐ Portal</h2><p class="muted">人超盃、夢想起飛、臨時帳號、優培、委任、考照資料集中在這裡。</p>${portalCards(6)}<a class="btn primary wide morePortal" href="?page=portal">＋ 更多活動</a></section>`)}
function recordCard(r){let fs=(r.friends||[]).map(f=>f.name).filter(Boolean).join('、');return `<div class="item"><div class="name">${r.name}｜+${r.points} 分</div><div class="muted">${periodLabel(periodById(r.periodId))}｜${r.team}｜${r.date}｜${r.note||r.type}${fs?'｜新朋友：'+fs:''}</div><div class="actions"><button class="btn small blue" onclick="viewRecord('${r.id}')">查看</button><button class="btn small primary" onclick="editRecord('${r.id}')">修改</button><button class="btn small red" onclick="delRecord('${r.id}')">刪除</button></div></div>`}
window.setMode=m=>{scoreMode=m;scoreState.items={};render()}
window.changeTeam=t=>{scoreState.team=t;scoreState.name=members(t)[0]||'';render()}
window.qty=(id,n)=>{scoreState.items[id]=Math.max(0,Number(scoreState.items[id]||0)+n);render()}
function qr(){let ev=activeEvent(),pid=periodOfDate().id,url=ev?location.origin+location.pathname+`?page=checkin&id=${ev.id}`:'';shell(`<section class="card"><h2>建立 QR 簽到</h2><form id="eventForm"><div class="item"><div class="name">🟢 本週｜${periodLabel(periodById(pid))}</div><div class="muted">QR 預設建立在本週。</div></div><input type="hidden" name="periodId" value="${pid}"><div class="grid2"><label class="field"><span>活動類型</span><select name="type">${data.rules.map(r=>`<option value="${r.id}">${r.name}（${r.points}分）</option>`).join('')}</select></label><label class="field"><span>活動日期</span><input type="date" name="date" value="${today()}"></label></div><label class="field"><span>活動名稱</span><input name="name" placeholder="例如：夢想起飛"></label><button class="btn primary wide">建立活動 QR</button></form></section><section class="card"><h2>目前 QR</h2>${ev?`<div class="qrbox"><p>${ev.name}</p><div class="muted">${periodLabel(periodById(ev.periodId))}</div><img src="https://api.qrserver.com/v1/create-qr-code/?size=320x320&data=${encodeURIComponent(url)}"><div class="actions" style="justify-content:center"><button class="btn primary" onclick="copyText('${url}')">複製連結</button><a class="btn" href="?page=checkin&id=${ev.id}">直接測試簽到</a></div></div>`:'<div class="empty">尚無活動</div>'}</section>${ev?attendanceTable(ev):''}<section class="card"><h2>活動列表</h2>${data.events.map(e=>`<div class="item"><div class="name">${e.name}</div><div class="muted">${periodLabel(periodById(e.periodId))}｜${rule(e.type).name}｜${e.active?'目前使用':'待用'}</div><div class="actions"><button class="btn small primary" onclick="setActive('${e.id}')">設為目前</button><button class="btn small red" onclick="delEvent('${e.id}')">刪除</button></div></div>`).join('')||'<div class="empty">尚無活動</div>'}</section>`)}
function attendanceTable(ev){let recs=eventRecords(ev.id),signed=new Set(recs.map(r=>r.name));let rowsHtml=people().map(p=>{let r=recs.find(x=>x.name===p.name),fs=(r?.friends||[]).map(f=>f.name).filter(Boolean).join('、');return `<tr><td>${r?'✅':'❌'}</td><td>${p.team}</td><td>${p.name}</td><td class="hideM">${r?r.date:'未簽'}</td><td>${fs||'-'}</td><td>${r?('+'+r.points):'-'}</td><td>${r?`<button class="btn small primary" onclick="editRecord('${r.id}')">修改</button>`:`<button class="btn small green" onclick="manualCheckin('${ev.id}','${p.name}')">補簽</button>`}</td></tr>`}).join('');return `<section class="card"><h2>簽到管理｜${ev.name}</h2><div class="stats"><div class="stat">應到<b>${people().length}</b></div><div class="stat">已到<b>${signed.size}</b></div><div class="stat">未到<b>${people().length-signed.size}</b></div><div class="stat">積分<b>${recs.reduce((n,r)=>n+Number(r.points||0),0)}</b></div></div><div class="tableWrap"><table class="table"><thead><tr><th>狀態</th><th>隊伍</th><th>姓名</th><th class="hideM">時間</th><th>新朋友</th><th>分數</th><th>操作</th></tr></thead><tbody>${rowsHtml}</tbody></table></div></section>`}
function checkin(){let id=new URLSearchParams(location.search).get('id'),ev=data.events.find(e=>e.id===id)||activeEvent();if(!ev)return shell('<section class="card"><h2>找不到活動</h2><a class="btn primary" href="?page=qr">回 QR 頁</a></section>');let r=rule(ev.type);friendCount=1;shell(`<section class="card"><h2>${ev.name}</h2><p class="muted">${periodLabel(periodById(ev.periodId))}｜簽到 +${r.points}${r.allowFriends?`｜新朋友 +${r.friendPoints||r.points}/位`:''}</p><form id="checkinForm" data-event="${ev.id}"><label class="field"><span>隊伍</span><select name="team" onchange="renderCheckinMembers(this.value)">${teamOptions(teams()[0]?.name)}</select></label><label class="field"><span>姓名</span><select name="name" id="checkinName">${memberOptions(teams()[0]?.name)}</select></label>${r.allowFriends?friendInputs():''}<button class="btn primary wide">完成簽到</button></form></section>`)}
function friendInputs(){let html=`<h3>新朋友名單</h3>`;for(let i=1;i<=friendCount;i++)html+=`<div class="friendBox"><label class="field"><span>新朋友 ${i} 姓名</span><input name="friendName${i}"></label><label class="field"><span>新朋友 ${i} 電話</span><input name="friendPhone${i}"></label></div>`;return html+`<button class="btn small" type="button" onclick="addFriend()">＋新增新朋友</button>`}
window.addFriend=()=>{friendCount++;render()}
window.renderCheckinMembers=t=>{document.getElementById('checkinName').innerHTML=memberOptions(t)}
function rank(){let pid=statOffset===null?periodOfDate().id:'w'+statOffset,rec=recordsByPeriod(pid),tr=rankTeams(rec),pr=rankPeople(rec);shell(`<section class="card"><h2>排行榜</h2><div class="item"><div class="name">🟢 ${periodLabel(periodById(pid))}</div><div class="actions"><button class="btn" onclick="statOffset=(statOffset===null?currentOffset():statOffset)-1;render()">⬅ 上一週</button><button class="btn primary" onclick="statOffset=currentOffset();render()">本週</button><button class="btn" onclick="statOffset=(statOffset===null?currentOffset():statOffset)+1;render()">下一週 ➡</button></div></div><div class="stats"><div class="stat">總分<b>${rec.reduce((n,r)=>n+Number(r.points||0),0)}</b></div><div class="stat">紀錄數<b>${rec.length}</b></div><div class="stat">新朋友<b>${rec.reduce((n,r)=>n+(r.friends||[]).length,0)}</b></div><div class="stat">第一名<b>${tr[0]?.team||'尚無'}</b></div></div></section><div class="grid2"><section class="card"><h2>團隊排行</h2>${rows(tr,'team')}</section><section class="card"><h2>個人排行</h2>${rows(pr,'person')}</section></div><section class="card"><h2>積分流水帳</h2>${rec.slice().reverse().map(recordCard).join('')||'<div class="empty">尚無紀錄</div>'}</section>`)}
function admin(){shell(`<section class="card"><h2>系統設定</h2><form id="brandForm"><div class="grid3"><label class="field"><span>主標題</span><input name="mainTitle" value="${data.settings.mainTitle}"></label><label class="field"><span>副標題</span><input name="subTitle" value="${data.settings.subTitle}"></label><label class="field"><span>標語</span><input name="slogan" value="${data.settings.slogan}"></label></div><label class="field"><span>首頁公告</span><textarea name="notice">${data.settings.notice||''}</textarea></label><button class="btn primary wide">儲存品牌文字</button></form></section><section class="card"><h2>日期區間設定</h2><form id="dateForm"><div class="grid3"><label class="field"><span>基準開始日期</span><input type="date" name="baseStart" value="${data.settings.baseStart}"></label><label class="field"><span>每週天數</span><input type="number" name="weekDays" value="${data.settings.weekDays||7}"></label><label class="field"><span>前/後顯示</span><input type="text" value="前 ${data.settings.showBefore||2} 週，後 ${data.settings.showAfter||5} 週" disabled></label></div><div class="grid2"><label class="field"><span>前面顯示幾週</span><input type="number" name="showBefore" value="${data.settings.showBefore||2}"></label><label class="field"><span>後面顯示幾週</span><input type="number" name="showAfter" value="${data.settings.showAfter||5}"></label></div><button class="btn orange wide">儲存日期設定</button><p class="muted">只要設定基準開始日期，系統會自動產生 7 天一期的日期區間。</p></form></section><section class="card"><h2>目前可選區間</h2>${visiblePeriods().map(p=>`<div class="item"><div class="name">${periodLabel(p)}${p.offset===currentOffset()?'（本週）':''}</div></div>`).join('')}</section><section class="card"><h2>隊伍與成員</h2><div class="grid2"><section><h3>新增隊伍</h3><form id="teamForm"><input name="team" placeholder="隊伍名稱"><button class="btn primary wide">新增</button></form>${data.teams.map(t=>`<div class="item"><div class="name">${t.name}</div><div class="actions"><button class="btn small blue" onclick="renameTeam('${t.id}')">改名</button><button class="btn small red" onclick="delTeam('${t.id}')">刪除</button></div></div>`).join('')}</section><section><h3>新增成員</h3><form id="memberForm"><select name="team">${data.teams.map(t=>`<option value="${t.id}">${t.name}</option>`).join('')}</select><input name="name" placeholder="姓名"><button class="btn primary wide">新增</button></form>${people().map(p=>`<span class="pill">${p.team}｜${p.name}</span>`).join('')}</section></div></section><section class="card"><h2>Portal 管理</h2><form id="portalForm"><div class="grid3"><label class="field"><span>排序</span><input type="number" name="order" placeholder="例如：7"></label><label class="field"><span>圖示</span><input name="icon" placeholder="例如：🎯"></label><label class="field"><span>名稱</span><input name="name" placeholder="活動名稱"></label></div><label class="field"><span>說明</span><input name="desc" placeholder="簡短說明"></label><label class="field"><span>封面圖片網址</span><input name="image" placeholder="例如：./assets/portal/xxx.jpeg 或 https://..."></label><label class="field"><span>內容摘要</span><textarea name="summary" placeholder="點進 Portal 後看到的介紹文字"></textarea></label><div class="grid2"><label class="field"><span>連結名稱</span><input name="linkTitle" placeholder="例如：PDF下載 / 報名表單"></label><label class="field"><span>連結網址</span><input name="linkUrl" placeholder="https://..."></label></div><label class="field"><span>是否顯示首頁六宮格</span><select name="featured"><option value="false">更多活動</option><option value="true" selected>首頁顯示</option></select></label><button class="btn primary wide">新增 Portal 項目</button></form>${portalItemsSorted().map(x=>`<div class="item"><div class="name">${x.icon||'⭐'} ${x.name}</div><div class="muted">排序 ${x.order||'-'}｜${x.featured?'首頁六宮格':'更多活動'}｜${x.desc||''}</div><div class="actions"><button class="btn small" onclick="movePortal('${x.id}',-1)">上移</button><button class="btn small" onclick="movePortal('${x.id}',1)">下移</button><button class="btn small" onclick="togglePortal('${x.id}')">${x.featured?'移到更多':'設為首頁'}</button><button class="btn small red" onclick="delPortal('${x.id}')">刪除</button></div></div>`).join('')}</section><section class="card"><h2>積分規則</h2>${data.rules.map(r=>`<div class="item"><div class="name">${r.name}</div><div class="grid3"><label class="field"><span>分數</span><input type="number" value="${r.points}" onchange="updateRule('${r.id}','points',this.value)"></label><label class="field"><span>新朋友分數</span><input type="number" value="${r.friendPoints||0}" onchange="updateRule('${r.id}','friendPoints',this.value)"></label><label class="field"><span>名稱</span><input value="${r.name}" onchange="updateRule('${r.id}','name',this.value)"></label></div></div>`).join('')}</section>`)}
function bindForms(){let f=document.getElementById('scoreForm');if(f)f.onsubmit=e=>{e.preventDefault();let fd=new FormData(f);scoreState.periodId=fd.get('periodId');scoreState.team=fd.get('team');scoreState.name=fd.get('name');scoreState.manual=Number(fd.get('manual')||0);scoreState.note=fd.get('note')||'';let total=scoreTotal();if(total===0)return toast('本次得分為 0');let rec={id:editId||uid('rec'),date:now(),createdDate:today(),periodId:scoreState.periodId,team:scoreState.team,name:scoreState.name,mode:scoreMode,items:structuredClone(scoreState.items),manual:scoreState.manual,points:total,note:scoreState.note||'手動積分',type:scoreMode==='team'?'團隊積分':'個人積分'};if(editId){let i=data.records.findIndex(x=>x.id===editId);if(i>=0)data.records[i]={...data.records[i],...rec};editId=null}else data.records.push(rec);scoreState={periodId:scoreState.periodId,team:scoreState.team,name:scoreState.name,items:{},manual:0,note:'',showPeriod:false};save();toast('已儲存');go('home')};let ef=document.getElementById('eventForm');if(ef)ef.onsubmit=e=>{e.preventDefault();let fd=new FormData(ef),r=rule(fd.get('type'));data.events.forEach(x=>x.active=false);data.events.push({id:uid('ev'),name:fd.get('name')||r.name,type:r.id,periodId:fd.get('periodId'),date:fd.get('date'),active:true,createdAt:now()});save();toast('活動已建立');go('qr')};let cf=document.getElementById('checkinForm');if(cf)cf.onsubmit=e=>{e.preventDefault();let fd=new FormData(cf),ev=data.events.find(x=>x.id===cf.dataset.event),name=fd.get('name');let friends=[];for(let i=1;i<=friendCount;i++){let fn=(fd.get('friendName'+i)||'').trim(),ph=(fd.get('friendPhone'+i)||'').trim();if(fn)friends.push({name:fn,phone:ph})}checkinMember(ev.id,name,friends,'QR簽到')};let bf=document.getElementById('brandForm');if(bf)bf.onsubmit=e=>{e.preventDefault();let fd=new FormData(bf);data.settings.mainTitle=fd.get('mainTitle');data.settings.subTitle=fd.get('subTitle');data.settings.slogan=fd.get('slogan');data.settings.notice=fd.get('notice');save();toast('文字已更新');render()};let df=document.getElementById('dateForm');if(df)df.onsubmit=e=>{e.preventDefault();let fd=new FormData(df);data.settings.baseStart=fd.get('baseStart');data.settings.weekDays=Number(fd.get('weekDays')||7);data.settings.showBefore=Number(fd.get('showBefore')||2);data.settings.showAfter=Number(fd.get('showAfter')||5);save();toast('日期設定已更新');render()};let tf=document.getElementById('teamForm');if(tf)tf.onsubmit=e=>{e.preventDefault();let v=new FormData(tf).get('team');if(v){data.teams.push({id:uid('t'),name:v,members:[]});save();render()}};let mf=document.getElementById('memberForm');if(mf)mf.onsubmit=e=>{e.preventDefault();let fd=new FormData(mf),t=data.teams.find(x=>x.id===fd.get('team'));if(t&&fd.get('name')){t.members.push(fd.get('name'));save();render()}};let pf=document.getElementById('portalForm');if(pf)pf.onsubmit=e=>{e.preventDefault();let fd=new FormData(pf),name=(fd.get('name')||'').trim();if(!name)return toast('請輸入名稱');data.portalItems=data.portalItems||[];let linkTitle=(fd.get('linkTitle')||'').trim(),linkUrl=(fd.get('linkUrl')||'').trim();let links=linkUrl?[{title:linkTitle||'開啟連結',url:linkUrl}]:[];data.portalItems.push({id:uid('portal'),order:Number(fd.get('order')||((data.portalItems||[]).length+1)),icon:fd.get('icon')||'⭐',name,desc:fd.get('desc')||'',image:fd.get('image')||'',summary:fd.get('summary')||'',featured:fd.get('featured')==='true',gallery:fd.get('image')?[fd.get('image')]:[],links});save();toast('Portal 已新增');render()}}
function checkinMember(eventId,name,friends=[],source='補簽'){let ev=data.events.find(x=>x.id===eventId),r=rule(ev.type);if(data.records.some(x=>x.eventId===eventId&&x.name===name))return toast('此活動已簽到');let pts=Number(r.points||0)+Number(friends.length||0)*Number(r.friendPoints||r.points||0);data.records.push({id:uid('rec'),eventId,date:now(),createdDate:today(),periodId:ev.periodId,team:teamOf(name),name,mode:'team',type:r.name,points:pts,friends,note:`${ev.name}｜${source}`,items:{[r.id]:1},manual:0});save();toast('簽到成功');go('home')}
window.manualCheckin=(eventId,name)=>checkinMember(eventId,name,[],'管理員補簽')
window.delRecord=id=>{let i=data.records.findIndex(r=>r.id===id);if(i>=0&&confirm('刪除此紀錄？')){data.records.splice(i,1);save();render()}}
window.viewRecord=id=>{let r=data.records.find(x=>x.id===id);if(r){let fs=(r.friends||[]).map(f=>`${f.name}${f.phone?'（'+f.phone+'）':''}`).join('、');alert(`姓名：${r.name}\n隊伍：${r.team}\n日期區間：${periodLabel(periodById(r.periodId))}\n分數：${r.points}\n新朋友：${fs||'無'}\n備註：${r.note}\n日期：${r.date}`)}}
window.editRecord=id=>{let r=data.records.find(x=>x.id===id);if(!r)return;editId=id;scoreMode=r.mode||'team';scoreState={periodId:r.periodId||periodOfDate().id,team:r.team,name:r.name,items:structuredClone(r.items||{}),manual:Number(r.manual||0),note:r.note||'',showPeriod:true};go('score')}
window.setActive=id=>{data.events.forEach(e=>e.active=e.id===id);save();render()}
window.delEvent=id=>{if(confirm('刪除活動？')){data.events=data.events.filter(e=>e.id!==id);save();render()}}
window.copyText=async t=>{try{await navigator.clipboard.writeText(t);toast('已複製')}catch(e){prompt('複製連結',t)}}
window.delTeam=id=>{if(confirm('刪除隊伍？')){data.teams=data.teams.filter(t=>t.id!==id);save();render()}}
window.renameTeam=id=>{let t=data.teams.find(x=>x.id===id),v=prompt('隊伍名稱',t.name);if(v){t.name=v;save();render()}}
window.updateRule=(id,key,v)=>{let r=rule(id);r[key]=key==='points'||key==='friendPoints'?Number(v||0):v;save();toast('已更新')}
function portal(){let id=new URLSearchParams(location.search).get('id'),items=portalItemsSorted(),item=items.find(x=>x.id===id);if(item){let gallery=(item.gallery&&item.gallery.length?item.gallery:(item.image?[item.image]:[]));let links=(item.links||[]).map(l=>`<a class="btn primary" href="${l.url}" target="_blank" rel="noopener">${l.title||'開啟連結'}</a>`).join('');return shell(`<section class="card portalDetailHero"><h2>${item.icon||'⭐'} ${item.name}</h2><p class="muted">${item.desc||''}</p>${item.image?`<img src="${item.image}" alt="${item.name}">`:''}<p>${item.summary||'可在管理中心補上 PDF、EDM、報名網址、YouTube 與說明文字。'}</p><div class="actions">${links||'<span class="muted">尚未設定下載或外部連結</span>'}</div></section><section class="card"><h2>🖼️ 圖文資料</h2><div class="galleryGrid">${gallery.map((g,i)=>`<a href="${g}" target="_blank"><img src="${g}" alt="${item.name} 圖片 ${i+1}"></a>`).join('')||'<div class="empty">尚無圖片</div>'}</div></section><section class="card"><h2>後續可增加</h2><div class="portalFeatureList"><span>📄 PDF</span><span>🎥 影片</span><span>🔗 報名連結</span><span>❓ FAQ</span><span>📢 最新公告</span></div></section>`)}let more=items.filter(x=>!x.featured);shell(`<section class="card"><h2>⭐ Portal</h2><p class="muted">主要六個入口固定顯示，其他活動可由管理中心新增。</p>${portalCards(6)}</section><section class="card"><h2>＋ 更多活動</h2>${more.map(x=>`<a class="item portalList" href="?page=portal&id=${x.id}"><div class="name">${x.icon||'⭐'} ${x.name}</div><div class="muted">${x.desc||''}</div></a>`).join('')||'<div class="empty">尚無更多活動，可到管理新增。</div>'}</section>`)}
window.movePortal=(id,dir)=>{let arr=portalItemsSorted(),i=arr.findIndex(x=>x.id===id),j=i+dir;if(i<0||j<0||j>=arr.length)return;[arr[i].order,arr[j].order]=[arr[j].order,arr[i].order];data.portalItems=arr;save();render()}
window.togglePortal=id=>{let x=(data.portalItems||[]).find(p=>p.id===id);if(x){x.featured=!x.featured;save();render()}}
window.delPortal=id=>{if(confirm('刪除此 Portal 項目？')){data.portalItems=(data.portalItems||[]).filter(p=>p.id!==id);save();render()}}
function render(){if(page==='home')home();else if(page==='score')score();else if(page==='qr')qr();else if(page==='checkin')checkin();else if(page==='rank')rank();else if(page==='portal')portal();else if(page==='admin')admin();else home()}
render();
