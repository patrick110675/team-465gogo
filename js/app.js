const firebaseConfig={
 apiKey:'AIzaSyCCBe5b_3jMHYSnwQwQr4r7uNdzm61PWBY',
 authDomain:'team-465.firebaseapp.com',
 projectId:'team-465',
 storageBucket:'team-465.firebasestorage.app',
 messagingSenderId:'1083534515383',
 appId:'1:1083534515383:web:b4f466e0ac36d42bc34132'
};
let db=null,auth=null,cloudReady=false,cloudSaving=false;
function cloudStatusText(){return cloudReady?'Firebase 已連線':'本機模式';}
const KEY='amr_platform_1_lightblue_v2_data';
const defaultData={
 settings:{mainTitle:'AMR',subTitle:'人力特攻隊',season:'2026 第三季競賽',slogan:'團結・合作・突破・共創榮耀',notice:'歡迎來到 AMR 人力特攻隊',baseStart:'2026-07-06',homeBannerImage:'',appBg1:'#f8fcff',appBg2:'#eaf6ff',cardBg:'#eaf6ffcc',cardSolid:'#f7fbff',cardTitleColor:'#0d4b9c',textColor:'#103158',mainTitleColor:'#2b8cff',subTitleColor:'#092a56',seasonColor:'#2677de',sloganColor:'#143c77',mainTitleSize:48,subTitleSize:32,seasonSize:24,sloganSize:18,portalTitleSize:20,portalSubTitleSize:12,portalTitleColor:'#ffffff',portalSubTitleColor:'#dceeff',cardRadius:26},
 teams:[
  {id:'t1',name:'五狼攏來隊',members:['天景','小怡君','立維','永朋']},
  {id:'t2',name:'瀚瀚瀚瀚得第一',members:['子瀚','靜萱','陳怡君','雅韻']},
  {id:'t3',name:'盈在起跑點',members:['可盈','恩慈','士誼','奎璿','美如']},
  {id:'t4',name:'陽光委任隊',members:['怡蒨','楚涵','永濂','瑀芯']},
  {id:'t5',name:'你來就隊',members:['巧云','乙榛','圜翰','建宏']}
 ],
 personalRules:[
  {id:'web_contact',name:'網增接觸 / 新增名單 / 約訪',points:1,unit:'次'},
  {id:'warm_cop_soft',name:'緣故約訪 COP / 軟性活動',points:1,unit:'次'},
  {id:'first_interview',name:'初次面談',points:5,unit:'次'},
  {id:'deep_interview',name:'深度面談',points:8,unit:'次'},
  {id:'self_cop',name:'自己參加 COP',points:8,unit:'次'},
  {id:'friend_cop',name:'帶新朋友參加 COP',points:8,unit:'位'}
 ],
 teamRules:[
  {id:'dream_attend',name:'夢想起飛 / 夜創出席',points:2,unit:'次'},
  {id:'dream_friend',name:'帶新朋友參加夢想起飛 / 夜創',points:3,unit:'人'},
  {id:'soft_attend',name:'通訊處軟性活動出席',points:2,unit:'次'},
  {id:'soft_friend',name:'帶新朋友參加軟性活動',points:3,unit:'人'},
  {id:'exam_pass',name:'公司考通過',points:3,unit:'人'},
  {id:'register_contract',name:'登錄 + 簽約',points:10,unit:'人'},
  {id:'excellent',name:'參加優培',points:10,unit:'人'},
  {id:'team_recruit',name:'團隊共同運作增員',points:1,unit:'次'}
 ],
 portalItems:[
  {id:'portal_supercup',order:1,name:'人力超級盃',icon:'🏆',desc:'競賽辦法・積分規則・獎勵制度',featured:true,image:'./assets/portal/supercup-1.jpeg',summary:'115年度人力發展超級盃，整合競賽辦法、積分制度、成功增員與獎勵重點。',gallery:['./assets/portal/supercup-1.jpeg','./assets/portal/supercup-2.jpeg','./assets/portal/supercup-3.jpeg','./assets/portal/supercup-4.jpeg','./assets/portal/supercup-5.jpeg'],links:[]},
  {id:'portal_dream',order:2,name:'夢想起飛',icon:'🚀',desc:'轉職故事・夜間 COP・活動報名',featured:true,image:'./assets/portal/dream-cover.jpeg',summary:'放置夢想起飛、夜間 COP、創業說明會、講師介紹與報名資訊。',gallery:['./assets/portal/dream-cover.jpeg'],links:[]},
  {id:'portal_newbie',order:3,name:'臨時帳號建立',icon:'🪪',desc:'新人帳號・公會登錄・優培申請流程',featured:true,image:'./assets/portal/newbie-cover.jpeg',summary:'整理新人臨時帳號、公會、登錄、優培申請流程，讓主管與新人快速查詢。',gallery:['./assets/portal/newbie-cover.jpeg'],links:[]},
  {id:'portal_excellent',order:4,name:'優培制度',icon:'🌱',desc:'115優質人才培訓・四階段培育計畫',featured:true,image:'./assets/portal/excellent-cover.jpeg',summary:'優培制度、公文、四階段條件、獎金制度與維持資格重點。',gallery:['./assets/portal/excellent-cover.jpeg'],links:[{title:'優培制度公文 PDF',url:'./assets/portal/excellent.pdf'}]},
  {id:'portal_appoint',order:5,name:'委任制度',icon:'👑',desc:'主任・襄理・區經理委任條件',featured:true,image:'./assets/portal/appoint-1.jpeg',summary:'整理業務主任、襄理、區經理委任條件與必修課程。',gallery:['./assets/portal/appoint-1.jpeg','./assets/portal/appoint-2.jpeg','./assets/portal/appoint-3.jpeg'],links:[]},
  {id:'portal_exam',order:6,name:'考照大補帖',icon:'📚',desc:'公輔班・考試日期・准考證與成績查詢',featured:true,image:'./assets/portal/exam-1.jpeg',summary:'整理新人公會、外幣、投資型測驗、公輔班、准考證與成績查詢資料。',gallery:['./assets/portal/exam-1.jpeg','./assets/portal/exam-2.jpeg','./assets/portal/exam-3.jpeg','./assets/portal/exam-4.jpeg','./assets/portal/exam-5.jpeg'],links:[]}
 ],
 announcements:[
  {id:'a1',type:'活動',title:'夢想起飛 7/17 開始報名！',date:'2026/07/07'},
  {id:'a2',type:'活動',title:'夜間 COP 本週三舉行',date:'2026/07/07'},
  {id:'a3',type:'公告',title:'人超盃最新公告',date:'2026/07/06'}
 ],
 events:[], checkins:[], scores:[], audit:[]
};
let data=load();
let page=new URLSearchParams(location.search).get('page')||'home';
let scoreMode='personal';
let rankTab='score';
let currentTeam=localStorage.getItem('amr_current_team')||data.teams[0].name;
let currentName=localStorage.getItem('amr_current_name')||data.teams[0].members[0];
function clone(x){return JSON.parse(JSON.stringify(x))}
function load(){try{let x=JSON.parse(localStorage.getItem(KEY));if(!x)return clone(defaultData);return {...clone(defaultData),...x,settings:{...defaultData.settings,...(x.settings||{})},portalItems:(x.portalItems&&x.portalItems.length?x.portalItems:defaultData.portalItems),announcements:(x.announcements&&x.announcements.length?x.announcements:defaultData.announcements)}}catch(e){return clone(defaultData)}}
function save(){localStorage.setItem(KEY,JSON.stringify(data));saveCloud()}
async function initCloud(){
 try{
  if(!window.firebase){console.warn('Firebase SDK 未載入');return}
  if(!firebase.apps.length)firebase.initializeApp(firebaseConfig);
  db=firebase.firestore();auth=firebase.auth();
  await auth.signInAnonymously().catch(()=>null);
  const snap=await db.collection('platform').doc('settings').get();
  const remote=snap.exists&&snap.data()&&snap.data().amrPlatformV1;
  if(remote){data={...clone(defaultData),...remote,settings:{...defaultData.settings,...(remote.settings||{})},portalItems:(remote.portalItems&&remote.portalItems.length?remote.portalItems:defaultData.portalItems),announcements:(remote.announcements&&remote.announcements.length?remote.announcements:defaultData.announcements)};localStorage.setItem(KEY,JSON.stringify(data));}
  cloudReady=true;if(!isEditingField())render();
 }catch(e){console.warn('Firebase 連線失敗',e);cloudReady=false}
}
async function saveCloud(){
 if(!db||cloudSaving)return;
 try{cloudSaving=true;await db.collection('platform').doc('settings').set({amrPlatformV1:data,updatedAt:firebase.firestore.FieldValue.serverTimestamp()},{merge:true})}
 catch(e){console.warn('Firebase 儲存失敗',e)}
 finally{cloudSaving=false}
}

function uid(p='id'){return p+'_'+Math.random().toString(36).slice(2,9)}
function today(){let d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function now(){return new Date().toLocaleString('zh-TW')}
function teams(){return data.teams||[]}
function people(){return teams().flatMap(t=>(t.members||[]).map(m=>({name:m,team:t.name,teamId:t.id})))}
function members(team){return teams().find(t=>t.name===team)?.members||[]}
function teamOf(name){return people().find(p=>p.name===name)?.team||''}
function teamOptions(sel=''){return teams().map(t=>`<option value="${t.name}" ${sel===t.name?'selected':''}>${t.name}</option>`).join('')}
function memberOptions(team,sel=''){return members(team).map(m=>`<option value="${m}" ${sel===m?'selected':''}>${m}</option>`).join('')}
function parseDate(s){let [y,m,d]=String(s||today()).split('-').map(Number);return new Date(y,m-1,d)}
function addDays(d,n){let x=new Date(d);x.setDate(x.getDate()+n);return x}
function fmtDate(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function daysBetween(a,b){return Math.floor((parseDate(b)-parseDate(a))/86400000)}
function periodOfDate(d=today()){let off=Math.floor(daysBetween(data.settings.baseStart,d)/7);let s=addDays(parseDate(data.settings.baseStart),off*7),e=addDays(s,6);return {id:'w'+off,start:fmtDate(s),end:fmtDate(e)}}
function thisMonth(d=today()){return String(d).slice(0,7)}
function quarterOf(d=today()){let x=parseDate(d);return `${x.getFullYear()}-Q${Math.floor(x.getMonth()/3)+1}`}
function toast(m){let t=document.createElement('div');t.className='toast';t.textContent=m;document.body.appendChild(t);setTimeout(()=>t.remove(),1600)}
function link(p){return `?page=${p}`}
function go(p){history.pushState(null,'',link(p));page=p;render()} window.go=go;
window.addEventListener('popstate',()=>{page=new URLSearchParams(location.search).get('page')||'home';render()});
document.addEventListener('click',e=>{let a=e.target.closest('a[href^="?page="]');if(!a)return;e.preventDefault();go(new URLSearchParams(a.getAttribute('href').slice(1)).get('page')||'home')},true);

function isEditingField(){
 const el=document.activeElement;
 return !!el && ['INPUT','TEXTAREA','SELECT'].includes(el.tagName);
}
function applyTheme(){let st=data.settings||{};let r=document.documentElement;r.style.setProperty('--bg',st.appBg2||'#eaf6ff');r.style.setProperty('--bg2',st.appBg1||'#f8fcff');r.style.setProperty('--card',st.cardBg||'#eaf6ffcc');r.style.setProperty('--cardSolid',st.cardSolid||'#f7fbff');r.style.setProperty('--text',st.textColor||'#103158');r.style.setProperty('--cardTitleColor',st.cardTitleColor||'#0d4b9c');r.style.setProperty('--mainTitleColor',st.mainTitleColor||'#2b8cff');r.style.setProperty('--subTitleColor',st.subTitleColor||'#092a56');r.style.setProperty('--seasonColor',st.seasonColor||'#2677de');r.style.setProperty('--sloganColor',st.sloganColor||'#143c77');r.style.setProperty('--mainTitleSize',(st.mainTitleSize||48)+'px');r.style.setProperty('--subTitleSize',(st.subTitleSize||32)+'px');r.style.setProperty('--seasonSize',(st.seasonSize||24)+'px');r.style.setProperty('--sloganSize',(st.sloganSize||18)+'px');r.style.setProperty('--portalTitleSize',(st.portalTitleSize||20)+'px');r.style.setProperty('--portalSubTitleSize',(st.portalSubTitleSize||12)+'px');r.style.setProperty('--portalTitleColor',st.portalTitleColor||'#ffffff');r.style.setProperty('--portalSubTitleColor',st.portalSubTitleColor||'#dceeff');r.style.setProperty('--cardRadius',(st.cardRadius||26)+'px')}
function esc(v=''){return String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('\"','&quot;')}
function shell(body){applyTheme();document.getElementById('app').innerHTML=body+fab();nav();bindForms()}
function hero(){let d=new Date();let img=data.settings.homeBannerImage?` style="--hero-img:url('${esc(data.settings.homeBannerImage)}')"`:'';return `<section class="topHero ${data.settings.homeBannerImage?'hasHeroImage':''}"${img}><div class="bell">🔔</div><div class="heroText"><div class="amr">${data.settings.mainTitle}</div><h1>${data.settings.subTitle}</h1><p class="season">${data.settings.season}</p></div><div class="heroArt"><span class="peak"></span></div><div class="heroMeta"><div class="datePill">📅 ${d.getFullYear()} / ${String(d.getMonth()+1).padStart(2,'0')} / ${String(d.getDate()).padStart(2,'0')} (${['日','一','二','三','四','五','六'][d.getDay()]})</div><div class="slogan">${data.settings.slogan}</div></div></section>`}
function nav(){let arr=[['home','🏠','首頁'],['qr','▦','QR'],['rank','🏆','排行榜'],['portal','⭐','Portal'],['admin','⚙️','管理']];document.getElementById('bottomNav').innerHTML=arr.map(x=>`<a href="${link(x[0])}" class="${page===x[0]?'active':''}"><span>${x[1]}</span>${x[2]}</a>`).join('')}
function fab(){return page==='checkin'?'':`<button type="button" class="fab" onclick="openScoreSheet()">＋</button><div id="sheetMask" class="sheetMask" onclick="closeScoreSheet()"></div><div id="scoreSheet" class="scoreSheet"><div class="sheetHandle"></div><h2>新增積分</h2><button type="button" onclick="startScore('personal')">👤 個人積分</button><button type="button" onclick="startScore('team')">👥 團隊積分</button><button type="button" class="cancel" onclick="closeScoreSheet()">取消</button></div>`}
window.openScoreSheet=()=>{document.getElementById('sheetMask')?.classList.add('show');document.getElementById('scoreSheet')?.classList.add('show')}
window.closeScoreSheet=()=>{document.getElementById('sheetMask')?.classList.remove('show');document.getElementById('scoreSheet')?.classList.remove('show')}
window.startScore=m=>{scoreMode=m;closeScoreSheet();go('score')}
function scoreRecords(){return data.scores.filter(s=>!s.deleted)}
function rankTeams(){let m={};teams().forEach(t=>m[t.name]=0);scoreRecords().forEach(s=>{if(s.team in m)m[s.team]+=Number(s.points||0)});return Object.entries(m).map(([team,score])=>({team,score})).sort((a,b)=>b.score-a.score)}
function rankPeople(){let m={};people().forEach(p=>m[p.name]={score:0,team:p.team});scoreRecords().filter(s=>s.type==='personal').forEach(s=>{if(!m[s.name])m[s.name]={score:0,team:s.team};m[s.name].score+=Number(s.points||0)});return Object.entries(m).map(([name,x])=>({name,team:x.team,score:x.score})).sort((a,b)=>b.score-a.score)}
function superCupRank(){return rankPeople().map(x=>({...x,superScore:x.score,done:x.score>=100})).sort((a,b)=>b.superScore-a.superScore)}
function rows(arr,type='team'){return arr.map((r,i)=>`<div class="rankItem"><div class="rankMedal">${i===0?'🥇':i===1?'🥈':i===2?'🥉':i+1}</div><div><div class="rankName">${type==='team'?r.team:r.name}</div><div class="rankSub">${type==='team'?'團隊':r.team||''}</div></div><div class="rankScore">${type==='super'?r.superScore:r.score} 分</div></div>`).join('')||'<div class="empty">尚無資料</div>'}
function recentRows(){return scoreRecords().slice().sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt))).slice(0,5).map(s=>`<div class="recordItem" style="display:grid;grid-template-columns:44px 1fr auto;align-items:center;gap:10px"><div>${s.type==='team'?'👥':'👤'}</div><div><div class="name">${s.type==='team'?s.team:s.name}</div><div class="muted">${s.note||s.source||'積分紀錄'}</div></div><div class="score">${s.points>0?'+':''}${s.points} 分</div></div>`).join('')||'<div class="empty">尚無紀錄</div>'}
function home(){let tr=rankTeams(),pr=rankPeople();shell(hero()+`<div class="grid2"><section class="card"><h2>🏆 團隊 TOP 3 <a class="topLink" href="?page=rank">查看全部 ›</a></h2><div class="rankList">${rows(tr.slice(0,3),'team')}</div></section><section class="card"><h2>👤 個人 TOP 3 <a class="topLink" href="?page=rank">查看全部 ›</a></h2><div class="rankList">${rows(pr.slice(0,3),'person')}</div></section></div><div class="grid2"><section class="card"><h2>📢 最新公告 <a class="topLink" href="?page=admin">查看更多 ›</a></h2>${data.announcements.slice(0,3).map(a=>`<div class="noticeItem"><span class="noticeTag">${a.type}</span><b>${a.title}</b><div class="muted">${a.date}</div></div>`).join('')}</section><section class="card"><h2>⚡ 最新積分紀錄 <a class="topLink" href="?page=rank">查看更多 ›</a></h2>${recentRows()}</section></div>`)}
function score(){let rules=scoreMode==='personal'?data.personalRules:data.teamRules;let counts={};rules.forEach(r=>counts[r.id]=0);shell(`<section class="card"><h2>➕ 積分登記</h2><div class="tabs"><button type="button" class="${scoreMode==='personal'?'active':''}" onclick="scoreMode='personal';render()">👤 個人積分</button><button type="button" class="${scoreMode==='team'?'active':''}" onclick="scoreMode='team';render()">👥 團隊積分</button></div><label class="field"><span>隊伍</span><select id="scoreTeam" onchange="changeScoreTeam(this.value)">${teamOptions(currentTeam)}</select></label>${scoreMode==='personal'?`<label class="field"><span>姓名</span><select id="scoreName" onchange="currentName=this.value;localStorage.setItem('amr_current_name',currentName)">${memberOptions(currentTeam,currentName)}</select></label>`:''}<div id="scoreRows">${rules.map(r=>scoreItem(r)).join('')}</div><div class="totalBox"><b>本次得分</b><strong><span id="scoreTotal">0</span> 分</strong></div><button type="button" class="btn primary wide" onclick="saveScoreEntry()">儲存本次紀錄</button></section>`);window.scoreCounts=counts;calcScore()}
function scoreItem(r){return `<div class="scoreRow" data-rule="${r.id}" data-points="${r.points}"><div><b>${r.name}</b><em>+${r.points} 分 / ${r.unit}</em></div><button type="button" class="roundBtn" onclick="scoreAdd('${r.id}',-1)">−</button><span class="count" id="cnt_${r.id}">0</span><button type="button" class="roundBtn" onclick="scoreAdd('${r.id}',1)">＋</button></div>`}
window.changeScoreTeam=v=>{currentTeam=v;localStorage.setItem('amr_current_team',v);let ms=members(v);if(!ms.includes(currentName)){currentName=ms[0]||'';localStorage.setItem('amr_current_name',currentName)};render()}
window.scoreAdd=(id,n)=>{window.scoreCounts[id]=Math.max(0,(window.scoreCounts[id]||0)+n);document.getElementById('cnt_'+id).textContent=window.scoreCounts[id];calcScore()}
function calcScore(){let total=0;document.querySelectorAll('.scoreRow').forEach(row=>{let id=row.dataset.rule;total+=(window.scoreCounts?.[id]||0)*Number(row.dataset.points)});let el=document.getElementById('scoreTotal');if(el)el.textContent=total}
window.saveScoreEntry=()=>{let rules=scoreMode==='personal'?data.personalRules:data.teamRules;let items=rules.map(r=>({id:r.id,name:r.name,count:window.scoreCounts?.[r.id]||0,points:r.points,total:(window.scoreCounts?.[r.id]||0)*r.points})).filter(x=>x.count>0);let total=items.reduce((s,x)=>s+x.total,0);if(total<=0)return toast('尚未加分');let rec={id:uid('score'),type:scoreMode,team:currentTeam,name:scoreMode==='personal'?currentName:'',items,points:total,source:'手動加分',registrar:'使用者',note:items.map(i=>`${i.name}x${i.count}`).join('、'),createdAt:now(),date:today(),deleted:false};data.scores.push(rec);data.audit.push({id:uid('aud'),scoreId:rec.id,action:'建立',time:now(),user:'使用者',detail:`+${total}`});save();toast('已儲存');render()}
function rank(){shell(hero()+`<section class="card"><h2>🏆 排行榜</h2><div class="tabs"><button type="button" class="${rankTab==='score'?'active':''}" onclick="rankTab='score';render()">🏆 積分排行</button><button type="button" class="${rankTab==='super'?'active':''}" onclick="rankTab='super';render()">🏅 人超盃</button><button type="button" class="${rankTab==='meeting'?'active':''}" onclick="rankTab='meeting';render()">📅 例會出席</button><button type="button" class="${rankTab==='history'?'active':''}" onclick="rankTab='history';render()">📜 歷史紀錄</button></div>${rankContent()}</section>`)}
function rankContent(){if(rankTab==='score')return `<div class="grid2"><section><h2>團隊排行</h2>${rows(rankTeams(),'team')}</section><section><h2>個人排行</h2>${rows(rankPeople(),'person')}</section></div>`;if(rankTab==='super'){let sr=superCupRank();let done=sr.filter(x=>x.done);return `<div class="card"><h2>🏅 人超盃分數</h2>${rows(sr,'super')}</div><div class="card"><h2>🎯 達成人員</h2>${done.map(x=>`<div class="item">✅ <b>${x.name}</b> <span class="muted">${x.team}</span> <span class="score">${x.superScore} 分</span></div>`).join('')||'<div class="empty">尚無達成人員</div>'}</div>`}if(rankTab==='meeting')return meetingStats();return historyTable()}
function meetingStats(){let meetings=data.events.filter(e=>e.type==='meeting');let html=people().map(p=>{let records=data.checkins.filter(c=>c.personType==='member'&&c.name===p.name&&meetings.some(e=>e.id===c.eventId));let attend=records.filter(c=>c.status==='present').length,leave=records.filter(c=>c.status==='leave').length;let total=meetings.length;let absent=Math.max(0,total-attend-leave);let ok=total>0&&absent===0;return `<div class="rankItem"><div class="rankMedal">${ok?'✅':'📅'}</div><div><div class="rankName">${p.name}</div><div class="rankSub">${p.team}｜出席 ${attend}｜請假 ${leave}｜缺席 ${absent}</div></div><div class="rankScore">${total?Math.round((attend+leave)/total*100):0}%</div></div>`}).join('');return html||'<div class="empty">尚無例會資料</div>'}
function historyTable(){let list=scoreRecords().slice().sort((a,b)=>String(b.createdAt).localeCompare(String(a.createdAt)));return `<div class="tableWrap"><table class="table"><thead><tr><th>日期</th><th>類型</th><th>隊伍</th><th>姓名</th><th>分數</th><th>登記人</th><th>備註</th><th>操作</th></tr></thead><tbody>${list.map(s=>`<tr><td>${s.createdAt}</td><td>${s.type==='team'?'團隊':'個人'}</td><td>${s.team}</td><td>${s.name||'-'}</td><td>${s.points}</td><td>${s.registrar||''}</td><td>${s.note||''}</td><td><button type="button" class="btn small" onclick="editScore('${s.id}')">修改</button> <button type="button" class="btn red small" onclick="deleteScore('${s.id}')">刪除</button></td></tr>`).join('')}</tbody></table></div>`}
window.editScore=id=>{let s=data.scores.find(x=>x.id===id);if(!s)return;let p=Number(prompt('修改總分',s.points));if(!Number.isFinite(p))return;data.audit.push({id:uid('aud'),scoreId:id,action:'修改',time:now(),user:'管理員',detail:`${s.points}→${p}`});s.points=p;s.note=(s.note||'')+'（管理員更正）';save();render()}
window.deleteScore=id=>{let s=data.scores.find(x=>x.id===id);if(!s||!confirm('刪除此筆積分紀錄？'))return;s.deleted=true;data.audit.push({id:uid('aud'),scoreId:id,action:'刪除',time:now(),user:'管理員',detail:`刪除 ${s.points}`});save();render()}
function eventLabel(type){return {meeting:'每週三例會',dream:'夢想起飛',night:'夜間 COP',soft:'軟性活動'}[type]||'活動'}
function isSoft(type){return ['dream','night','soft'].includes(type)}
function activeEvent(){return data.events.find(e=>e.active)||data.events[0]}
function eventCheckins(id){return data.checkins.filter(c=>c.eventId===id)}
function qr(){let ev=activeEvent();shell(hero()+`<section class="card"><h2>📱 QR 簽到</h2><div class="grid2"><form id="eventForm" class="item"><label class="field"><span>活動類型</span><select name="type"><option value="meeting">每週三例會</option><option value="dream">夢想起飛</option><option value="night">夜間 COP</option><option value="soft">軟性活動</option></select></label><label class="field"><span>日期</span><input name="date" type="date" value="${today()}"></label><label class="field"><span>時間</span><input name="time" value="18:30"></label><label class="field"><span>地點</span><input name="place" value="AMR"></label><button class="btn primary wide">建立 QR</button></form><div class="item qrbox"><h2>${ev?eventLabel(ev.type):'尚無活動'}</h2>${ev?`<img src="https://api.qrserver.com/v1/create-qr-code/?size=280x280&data=${encodeURIComponent(location.origin+location.pathname+'?page=checkin&event='+ev.id)}"><button type="button" class="btn wide" onclick="copyText('${location.origin+location.pathname}?page=checkin&event=${ev.id}')">複製簽到連結</button>`:'<div class="empty">請先建立活動</div>'}</div></div></section><section class="card"><h2>活動紀錄</h2>${data.events.slice().reverse().map(e=>`<div class="item"><b>${eventLabel(e.type)}</b> <span class="muted">${e.date} ${e.time}</span><div class="muted">出席 ${eventCheckins(e.id).filter(c=>c.status==='present').length}｜新朋友 ${eventCheckins(e.id).filter(c=>c.personType==='friend').length}</div><div class="actions"><button type="button" class="btn small" onclick="setActive('${e.id}')">設為目前</button><button type="button" class="btn small" onclick="goCheck('${e.id}')">開啟簽到頁</button><button type="button" class="btn red small" onclick="delEvent('${e.id}')">刪除</button></div></div>`).join('')||'<div class="empty">尚無活動</div>'}</section>`)}
function checkin(){let id=new URLSearchParams(location.search).get('event'),ev=data.events.find(e=>e.id===id);if(!ev)return shell(hero()+`<section class="card"><h2>找不到活動</h2></section>`);shell(`<section class="card"><h2>AMR｜${eventLabel(ev.type)}</h2><div class="muted">${ev.date} ${ev.time}｜${ev.place||'AMR'}</div><form id="checkinForm"><input type="hidden" name="eventId" value="${ev.id}"><div class="checkType"><label><input type="radio" name="personType" value="member" checked>夥伴</label>${isSoft(ev.type)?'<label><input type="radio" name="personType" value="friend">新朋友</label>':''}</div><div id="memberFields"><label class="field"><span>隊伍</span><select name="team" onchange="updateCheckMembers(this.value)">${teamOptions(currentTeam)}</select></label><label class="field"><span>姓名</span><select name="name" id="checkMemberName">${memberOptions(currentTeam,currentName)}</select></label>${ev.type==='meeting'?'<label class="field"><span>狀態</span><select name="status"><option value="present">出席</option><option value="leave">請假</option></select></label>':''}</div><div id="friendFields" style="display:none"><label class="field"><span>新朋友姓名</span><input name="friendName"></label><label class="field"><span>電話</span><input name="phone"></label><label class="field"><span>介紹人</span><select name="introducer">${people().map(p=>`<option value="${p.name}">${p.name}｜${p.team}</option>`).join('')}</select></label></div><button class="btn primary wide">完成簽到</button></form></section>`)}
function portal(){let id=new URLSearchParams(location.search).get('id');if(id){let p=data.portalItems.find(x=>x.id===id);if(!p)return shell(hero()+`<section class="card">找不到 Portal</section>`);shell(hero()+`<section class="card portalHero"><h2>${p.icon} ${p.name}</h2><p>${p.summary||p.desc||''}</p>${p.image?`<img src="${p.image}">`:''}</section><section class="card"><h2>🖼 圖片資料</h2><div class="galleryGrid">${(p.gallery||[]).map(img=>`<a href="${img}" target="_blank"><img src="${img}"></a>`).join('')||'<div class="empty">尚無圖片</div>'}</div></section><section class="card"><h2>📄 連結 / PDF</h2>${(p.links||[]).map(l=>`<a class="btn wide" target="_blank" href="${l.url}">${l.title}</a>`).join('')||'<div class="empty">尚無連結</div>'}</section>`);return}shell(hero()+`<section class="card"><h2>⭐ Portal</h2><p class="muted">點選六大入口後，再查看圖片、PDF、影片與資料。</p><div class="portalGrid">${portalCards()}</div></section>`)}
function portalCards(){return data.portalItems.slice().sort((a,b)=>Number(a.order)-Number(b.order)).filter(x=>x.featured).map((x,i)=>`<a class="portalCard" href="?page=portal&id=${x.id}"><span class="portalBadge">${String(i+1).padStart(2,'0')}</span>${x.image?`<img src="${x.image}">`:''}<div class="portalShade"><b>${x.icon} ${x.name}</b><em>${x.desc}</em></div></a>`).join('')}

function portalAdminList(){return `<div class="portalAdminList">${data.portalItems.slice().sort((a,b)=>Number(a.order)-Number(b.order)).map(p=>`<form class="portalEdit" data-id="${p.id}"><div class="portalEditHead"><b>${p.icon||'⭐'} ${p.name}</b><span>排序 ${p.order||''}</span></div><div class="miniGrid"><label class="field"><span>名稱</span><input name="name" value="${esc(p.name||'')}"></label><label class="field"><span>Icon</span><input name="icon" value="${esc(p.icon||'⭐')}"></label><label class="field"><span>排序</span><input type="number" name="order" value="${p.order||1}"></label><label class="field"><span>顯示</span><select name="featured"><option value="true" ${p.featured?'selected':''}>顯示</option><option value="false" ${!p.featured?'selected':''}>隱藏</option></select></label></div><label class="field"><span>副標題</span><input name="desc" value="${esc(p.desc||'')}"></label><label class="field"><span>封面圖片網址</span><input name="image" value="${esc(p.image||'')}" placeholder="https://...jpg 或 ./assets/..."></label><label class="field"><span>介紹文字</span><textarea name="summary">${esc(p.summary||'')}</textarea></label><button class="btn primary wide">儲存這張 Portal</button></form>`).join('')}</div>`}
function admin(){shell(hero()+`<section class="card"><h2>⚙️ 管理中心</h2><div class="muted">☁️ Firebase 狀態：${cloudStatusText()}</div><div class="grid2"><form id="settingsForm" class="item"><h2>首頁設定 / 版面設計</h2><label class="field"><span>主標題</span><input name="mainTitle" value="${esc(data.settings.mainTitle)}"></label><label class="field"><span>副標題</span><input name="subTitle" value="${esc(data.settings.subTitle)}"></label><label class="field"><span>競賽文字</span><input name="season" value="${esc(data.settings.season)}"></label><label class="field"><span>公告</span><textarea name="notice">${esc(data.settings.notice)}</textarea></label><label class="field"><span>首頁 Banner 圖片網址（可留空）</span><input name="homeBannerImage" value="${esc(data.settings.homeBannerImage||'')}" placeholder="https://...jpg"></label><div class="miniGrid"><label class="field"><span>主標題大小</span><input type="number" name="mainTitleSize" value="${data.settings.mainTitleSize||48}"></label><label class="field"><span>副標題大小</span><input type="number" name="subTitleSize" value="${data.settings.subTitleSize||32}"></label><label class="field"><span>競賽文字大小</span><input type="number" name="seasonSize" value="${data.settings.seasonSize||24}"></label><label class="field"><span>標語大小</span><input type="number" name="sloganSize" value="${data.settings.sloganSize||18}"></label></div><div class="miniGrid"><label class="field"><span>主標題顏色</span><input type="color" name="mainTitleColor" value="${data.settings.mainTitleColor||'#2b8cff'}"></label><label class="field"><span>副標題顏色</span><input type="color" name="subTitleColor" value="${data.settings.subTitleColor||'#092a56'}"></label><label class="field"><span>競賽文字顏色</span><input type="color" name="seasonColor" value="${data.settings.seasonColor||'#2677de'}"></label><label class="field"><span>標語顏色</span><input type="color" name="sloganColor" value="${data.settings.sloganColor||'#143c77'}"></label></div><div class="miniGrid"><label class="field"><span>背景上色</span><input type="color" name="appBg1" value="${data.settings.appBg1||'#f8fcff'}"></label><label class="field"><span>背景下色</span><input type="color" name="appBg2" value="${data.settings.appBg2||'#eaf6ff'}"></label><label class="field"><span>方框顏色</span><input type="color" name="cardSolid" value="${data.settings.cardSolid||'#f7fbff'}"></label><label class="field"><span>方框標題顏色</span><input type="color" name="cardTitleColor" value="${data.settings.cardTitleColor||'#0d4b9c'}"></label></div><label class="field"><span>方框圓角</span><input type="number" name="cardRadius" value="${data.settings.cardRadius||26}"></label><button class="btn primary wide">儲存設定</button></form><form id="announceForm" class="item"><h2>新增公告</h2><label class="field"><span>類型</span><input name="type" value="公告"></label><label class="field"><span>標題</span><input name="title"></label><button class="btn primary wide">新增公告</button></form></div><form id="portalStyleForm" class="cardInner"><h2>⭐ Portal 文字樣式</h2><div class="miniGrid"><label class="field"><span>標題大小</span><input type="number" name="portalTitleSize" value="${data.settings.portalTitleSize||20}"></label><label class="field"><span>副標題大小</span><input type="number" name="portalSubTitleSize" value="${data.settings.portalSubTitleSize||12}"></label><label class="field"><span>標題顏色</span><input type="color" name="portalTitleColor" value="${data.settings.portalTitleColor||'#ffffff'}"></label><label class="field"><span>副標題顏色</span><input type="color" name="portalSubTitleColor" value="${data.settings.portalSubTitleColor||'#dceeff'}"></label></div><button class="btn primary wide">儲存 Portal 樣式</button></form><section class="cardInner"><h2>⭐ Portal 圖片 / 文字管理</h2>${portalAdminList()}</section><div class="actions"><button type="button" class="btn" onclick="exportAll()">匯出資料</button><button type="button" class="btn red" onclick="resetAll()">恢復預設</button></div></section>`)}
function bindForms(){let sf=document.getElementById('settingsForm');if(sf)sf.onsubmit=e=>{e.preventDefault();let f=new FormData(sf);['mainTitle','subTitle','season','notice','homeBannerImage','mainTitleColor','subTitleColor','seasonColor','sloganColor','appBg1','appBg2','cardSolid','cardTitleColor'].forEach(k=>data.settings[k]=f.get(k));['mainTitleSize','subTitleSize','seasonSize','sloganSize','cardRadius'].forEach(k=>data.settings[k]=Number(f.get(k))||data.settings[k]);data.settings.cardBg=(data.settings.cardSolid||'#f7fbff')+'cc';save();toast('已儲存');render()};let psf=document.getElementById('portalStyleForm');if(psf)psf.onsubmit=e=>{e.preventDefault();let f=new FormData(psf);['portalTitleColor','portalSubTitleColor'].forEach(k=>data.settings[k]=f.get(k));['portalTitleSize','portalSubTitleSize'].forEach(k=>data.settings[k]=Number(f.get(k))||data.settings[k]);save();toast('已儲存 Portal 樣式');render()};document.querySelectorAll('.portalEdit').forEach(form=>form.onsubmit=e=>{e.preventDefault();let id=form.dataset.id,p=data.portalItems.find(x=>x.id===id);if(!p)return;let f=new FormData(form);['name','icon','desc','image','summary'].forEach(k=>p[k]=f.get(k));p.order=Number(f.get('order'))||p.order;p.featured=f.get('featured')==='true';save();toast('已更新 Portal');render()});let af=document.getElementById('announceForm');if(af)af.onsubmit=e=>{e.preventDefault();let f=new FormData(af);data.announcements.unshift({id:uid('ann'),type:f.get('type')||'公告',title:f.get('title')||'新公告',date:today().replaceAll('-','/')});save();toast('已新增公告');render()};let ef=document.getElementById('eventForm');if(ef)ef.onsubmit=e=>{e.preventDefault();let f=new FormData(ef);data.events.forEach(x=>x.active=false);let ev={id:uid('evt'),brand:'AMR',type:f.get('type'),date:f.get('date'),time:f.get('time'),place:f.get('place'),active:true,createdAt:now()};data.events.push(ev);save();toast('已建立 QR');render()};let cf=document.getElementById('checkinForm');if(cf){cf.onchange=e=>{if(e.target.name==='personType'){document.getElementById('memberFields').style.display=e.target.value==='member'?'block':'none';document.getElementById('friendFields').style.display=e.target.value==='friend'?'block':'none'}};cf.onsubmit=e=>{e.preventDefault();let f=new FormData(cf),ev=data.events.find(x=>x.id===f.get('eventId'));let type=f.get('personType');let rec={id:uid('chk'),eventId:ev.id,eventType:ev.type,date:ev.date,time:now(),personType:type,status:f.get('status')||'present'};if(type==='friend'){rec.name=f.get('friendName');rec.phone=f.get('phone');rec.introducer=f.get('introducer');rec.team=teamOf(rec.introducer)}else{rec.name=f.get('name');rec.team=f.get('team')}data.checkins.push(rec);if(isSoft(ev.type)&&rec.status==='present'){autoScoreFromCheckin(ev,rec)}save();toast('簽到完成');go('qr')}}}
function autoScoreFromCheckin(ev,rec){let points=0,note='';if(rec.personType==='member'){points=ev.type==='soft'?2:2;note=`${eventLabel(ev.type)}出席`}else{points=ev.type==='soft'?3:3;note=`帶新朋友參加${eventLabel(ev.type)}`;rec.name=rec.name||'新朋友'}let name=rec.personType==='friend'?rec.introducer:rec.name;data.scores.push({id:uid('score'),type:'personal',team:teamOf(name)||rec.team,name,items:[],points,source:'QR自動加分',registrar:'QR',note,date:ev.date,createdAt:now(),deleted:false})}
window.updateCheckMembers=team=>{document.getElementById('checkMemberName').innerHTML=memberOptions(team,members(team)[0])}
window.copyText=t=>navigator.clipboard?.writeText(t).then(()=>toast('已複製')).catch(()=>prompt('複製連結',t))
window.setActive=id=>{data.events.forEach(e=>e.active=e.id===id);save();render()}
window.goCheck=id=>{history.pushState(null,'',`?page=checkin&event=${id}`);page='checkin';render()}
window.delEvent=id=>{if(confirm('刪除活動？')){data.events=data.events.filter(e=>e.id!==id);data.checkins=data.checkins.filter(c=>c.eventId!==id);save();render()}}
window.exportAll=()=>{let blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});let a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='amr-platform-backup.json';a.click()}
window.resetAll=()=>{if(confirm('恢復預設會清除目前資料，確定？')){localStorage.removeItem(KEY);data=load();render()}}
function render(){if(page==='home')home();else if(page==='score')score();else if(page==='qr')qr();else if(page==='checkin')checkin();else if(page==='rank')rank();else if(page==='portal')portal();else if(page==='admin')admin();else home()}
render();
initCloud();
